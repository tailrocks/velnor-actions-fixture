# Generated by velnor-workflow. Regenerate; do not hand-edit.
schema = 3
repository = "tailrocks/velnor-actions-fixture"
profile = "generic"
verified = true
default_branch = "main"
providers = ["github-hosted"]
automatic_providers = ["github-hosted"]
default_dispatch_providers = ["github-hosted"]

[analysis]
method = "static-filesystem-and-manifest-inspection"
detected = ["cargo-lockfile", "mise-present", "renovate-configuration", "rust-binaries:app-a:1", "rust-binaries:app-b:1", "rust-binaries:l2-contract:1", "rust-binaries:verifier:1", "rust-build-script:fixture-harness", "rust-test-targets:fixture-harness:1", "rust-test-targets:verifier:1", "rust-workspace-members:6"]
limitations = ["Project code, build scripts, task runners, and commands are never executed during analysis.", "Release, signing, registry, deployment, branch-protection, and runner-capability contracts remain explicit manual inputs.", "Renovate credentials, runner placement, and write permissions cannot be inferred from repository files.", "Rust build scripts are watched but their system dependencies and generated outputs are not inferred."]

[workflow]
files = ["ci-main.yml", "ci-policy.yml", "ci-pr.yml", "ci-unit-docker.yml", "ci-unit-rust.yml", "maintenance.yml", "nightly.yml"]
notes = ["Rust verification uses Mr. Boxington 1.12.0 on providers [github-hosted]. GitHub-hosted jobs use its GitHub cache backend with bounded snapshot keys (a key names the toolchain/image/linker compatibility class and the hashed source state, so a new source state saves a new snapshot and retention keeps generations bounded). Local-provider jobs use an explicit local-backend setup step against the image/runner-provided local store."]

[release]
enabled = false
reason = "Release is fail-closed. Enable only after declaring immutable artifact, registry, provenance, and tag-protection policy."

[[unit]]
id = "docker"
label = "Docker"
kind = "docker"
root = "."
watch = ["**"]
pr_commands = ["docker build --file 'Dockerfile.fixture' --tag local-ci:dockerfile-fixture '.'"]
full_commands = ["docker build --file 'Dockerfile.fixture' --tag local-ci:dockerfile-fixture '.'"]
platform = "linux-x64"
trust = "untrusted-ok"
[unit.capabilities]
docker = true
nested_privileged_docker = false
buildx_compose = true
testcontainers = false
services_with_readiness = false
browser_binaries = false
native_macos_arm64 = false

[[unit]]
id = "rust-app-a"
label = "Rust crate (app-a)"
kind = "rust"
root = "crates/app-a"
watch = [".cargo/**", "Cargo.lock", "Cargo.toml", "crates/app-a/**/*.rs", "crates/app-a/Cargo.toml", "crates/app-a/benches/**", "crates/app-a/examples/**", "crates/app-a/src/**", "crates/app-a/tests/**", "mise.lock", "mise.toml", "rust-toolchain", "rust-toolchain.toml"]
pr_commands = ["cd -- 'crates/app-a' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/app-a' && mbx clippy --locked --profile test --all-targets --all-features --package 'app-a' -- -D warnings", "cd -- 'crates/app-a' && mbx test --locked --all-features --package 'app-a'"]
full_commands = ["cd -- 'crates/app-a' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/app-a' && mbx clippy --locked --profile test --all-targets --all-features --package 'app-a' -- -D warnings", "cd -- 'crates/app-a' && mbx test --locked --all-features --package 'app-a'"]
phases = ["fmt", "clippy", "test"]
check_commands = ["cd -- 'crates/app-a' && mbx check --locked --profile test --all-targets --all-features --package 'app-a'"]
depends_on = ["rust-shared"]
platform = "linux-x64"
trust = "untrusted-ok"
[unit.capabilities]
docker = true
nested_privileged_docker = false
buildx_compose = false
testcontainers = true
services_with_readiness = false
browser_binaries = false
native_macos_arm64 = false
[unit.cache]
key_files = [".cargo/**", "Cargo.lock", "rust-toolchain.toml", "rust-toolchain"]
paths = ["~/.cargo/registry", "~/.cargo/git"]

[[unit]]
id = "rust-app-b"
label = "Rust crate (app-b)"
kind = "rust"
root = "crates/app-b"
watch = [".cargo/**", "Cargo.lock", "Cargo.toml", "crates/app-b/**/*.rs", "crates/app-b/Cargo.toml", "crates/app-b/benches/**", "crates/app-b/examples/**", "crates/app-b/src/**", "crates/app-b/tests/**", "mise.lock", "mise.toml", "rust-toolchain", "rust-toolchain.toml"]
pr_commands = ["cd -- 'crates/app-b' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/app-b' && mbx clippy --locked --profile test --all-targets --all-features --package 'app-b' -- -D warnings", "cd -- 'crates/app-b' && mbx test --locked --all-features --package 'app-b'"]
full_commands = ["cd -- 'crates/app-b' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/app-b' && mbx clippy --locked --profile test --all-targets --all-features --package 'app-b' -- -D warnings", "cd -- 'crates/app-b' && mbx test --locked --all-features --package 'app-b'"]
phases = ["fmt", "clippy", "test"]
check_commands = ["cd -- 'crates/app-b' && mbx check --locked --profile test --all-targets --all-features --package 'app-b'"]
depends_on = ["rust-shared"]
platform = "linux-x64"
trust = "untrusted-ok"
[unit.capabilities]
docker = true
nested_privileged_docker = false
buildx_compose = false
testcontainers = true
services_with_readiness = false
browser_binaries = false
native_macos_arm64 = false
[unit.cache]
key_files = [".cargo/**", "Cargo.lock", "rust-toolchain.toml", "rust-toolchain"]
paths = ["~/.cargo/registry", "~/.cargo/git"]

[[unit]]
id = "rust-fixture-harness"
label = "Rust crate (fixture-harness)"
kind = "rust"
root = "crates/fixture-harness"
watch = [".cargo/**", "Cargo.lock", "Cargo.toml", "crates/fixture-harness/**/*.rs", "crates/fixture-harness/Cargo.toml", "crates/fixture-harness/benches/**", "crates/fixture-harness/build.rs", "crates/fixture-harness/examples/**", "crates/fixture-harness/src/**", "crates/fixture-harness/tests/**", "mise.lock", "mise.toml", "rust-toolchain", "rust-toolchain.toml"]
pr_commands = ["cd -- 'crates/fixture-harness' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/fixture-harness' && mbx clippy --locked --profile test --all-targets --all-features --package 'fixture-harness' -- -D warnings", "cd -- 'crates/fixture-harness' && mbx test --locked --all-features --package 'fixture-harness'"]
full_commands = ["cd -- 'crates/fixture-harness' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/fixture-harness' && mbx clippy --locked --profile test --all-targets --all-features --package 'fixture-harness' -- -D warnings", "cd -- 'crates/fixture-harness' && mbx test --locked --all-features --package 'fixture-harness'"]
phases = ["fmt", "clippy", "test"]
check_commands = ["cd -- 'crates/fixture-harness' && mbx check --locked --profile test --all-targets --all-features --package 'fixture-harness'"]
platform = "linux-x64"
trust = "untrusted-ok"
[unit.capabilities]
docker = true
nested_privileged_docker = false
buildx_compose = false
testcontainers = true
services_with_readiness = false
browser_binaries = false
native_macos_arm64 = false
[unit.cache]
key_files = [".cargo/**", "Cargo.lock", "rust-toolchain.toml", "rust-toolchain"]
paths = ["~/.cargo/registry", "~/.cargo/git"]

[[unit]]
id = "rust-l2-contract"
label = "Rust crate (l2-contract)"
kind = "rust"
root = "crates/l2-contract"
watch = [".cargo/**", "Cargo.lock", "Cargo.toml", "crates/l2-contract/**/*.rs", "crates/l2-contract/Cargo.toml", "crates/l2-contract/benches/**", "crates/l2-contract/examples/**", "crates/l2-contract/src/**", "crates/l2-contract/tests/**", "fixtures/l2/closure.json", "fixtures/l2/invalid-mise.lock", "fixtures/l2/mutable-ref.json", "fixtures/l2/unknown-input.json", "fixtures/l2/unknown-repository.json", "fixtures/l2/unknown-subpath.json", "fixtures/l2/unresolved-expression.json", "mise.lock", "mise.toml", "rust-toolchain", "rust-toolchain.toml"]
pr_commands = ["cd -- 'crates/l2-contract' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/l2-contract' && mbx clippy --locked --profile test --all-targets --all-features --package 'l2-contract' -- -D warnings", "cd -- 'crates/l2-contract' && mbx test --locked --all-features --package 'l2-contract'"]
full_commands = ["cd -- 'crates/l2-contract' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/l2-contract' && mbx clippy --locked --profile test --all-targets --all-features --package 'l2-contract' -- -D warnings", "cd -- 'crates/l2-contract' && mbx test --locked --all-features --package 'l2-contract'"]
phases = ["fmt", "clippy", "test"]
check_commands = ["cd -- 'crates/l2-contract' && mbx check --locked --profile test --all-targets --all-features --package 'l2-contract'"]
platform = "linux-x64"
trust = "untrusted-ok"
[unit.capabilities]
docker = true
nested_privileged_docker = false
buildx_compose = false
testcontainers = true
services_with_readiness = false
browser_binaries = false
native_macos_arm64 = false
[unit.cache]
key_files = [".cargo/**", "Cargo.lock", "rust-toolchain.toml", "rust-toolchain"]
paths = ["~/.cargo/registry", "~/.cargo/git"]

[[unit]]
id = "rust-shared"
label = "Rust crate (shared)"
kind = "rust"
root = "crates/shared"
watch = [".cargo/**", "Cargo.lock", "Cargo.toml", "crates/shared/**/*.rs", "crates/shared/Cargo.toml", "crates/shared/benches/**", "crates/shared/examples/**", "crates/shared/src/**", "crates/shared/tests/**", "mise.lock", "mise.toml", "rust-toolchain", "rust-toolchain.toml"]
pr_commands = ["cd -- 'crates/shared' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/shared' && mbx clippy --locked --profile test --all-targets --all-features --package 'shared' -- -D warnings", "cd -- 'crates/shared' && mbx test --locked --all-features --package 'shared'"]
full_commands = ["cd -- 'crates/shared' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/shared' && mbx clippy --locked --profile test --all-targets --all-features --package 'shared' -- -D warnings", "cd -- 'crates/shared' && mbx test --locked --all-features --package 'shared'"]
phases = ["fmt", "clippy", "test"]
check_commands = ["cd -- 'crates/shared' && mbx check --locked --profile test --all-targets --all-features --package 'shared'"]
platform = "linux-x64"
trust = "untrusted-ok"
[unit.capabilities]
docker = true
nested_privileged_docker = false
buildx_compose = false
testcontainers = true
services_with_readiness = false
browser_binaries = false
native_macos_arm64 = false
[unit.cache]
key_files = [".cargo/**", "Cargo.lock", "rust-toolchain.toml", "rust-toolchain"]
paths = ["~/.cargo/registry", "~/.cargo/git"]

[[unit]]
id = "rust-verifier"
label = "Rust crate (verifier)"
kind = "rust"
root = "crates/verifier"
watch = [".cargo/**", "Cargo.lock", "Cargo.toml", "crates/verifier/**/*.rs", "crates/verifier/Cargo.toml", "crates/verifier/benches/**", "crates/verifier/examples/**", "crates/verifier/src/**", "crates/verifier/tests/**", "mise.lock", "mise.toml", "rust-toolchain", "rust-toolchain.toml"]
pr_commands = ["cd -- 'crates/verifier' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/verifier' && mbx clippy --locked --profile test --all-targets --all-features --package 'verifier' -- -D warnings", "cd -- 'crates/verifier' && mbx test --locked --all-features --package 'verifier'"]
full_commands = ["cd -- 'crates/verifier' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/verifier' && mbx clippy --locked --profile test --all-targets --all-features --package 'verifier' -- -D warnings", "cd -- 'crates/verifier' && mbx test --locked --all-features --package 'verifier'"]
phases = ["fmt", "clippy", "test"]
check_commands = ["cd -- 'crates/verifier' && mbx check --locked --profile test --all-targets --all-features --package 'verifier'"]
platform = "linux-x64"
trust = "untrusted-ok"
[unit.capabilities]
docker = true
nested_privileged_docker = false
buildx_compose = false
testcontainers = true
services_with_readiness = false
browser_binaries = false
native_macos_arm64 = false
[unit.cache]
key_files = [".cargo/**", "Cargo.lock", "rust-toolchain.toml", "rust-toolchain"]
paths = ["~/.cargo/registry", "~/.cargo/git"]
