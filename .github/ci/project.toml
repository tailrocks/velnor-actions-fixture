# Generated by velnor-workflow. Regenerate; do not hand-edit.
schema = 2
repository = "tailrocks/velnor-actions-fixture"
profile = "generic"
verified = true
default_branch = "main"
runners = "both"

[analysis]
method = "static-filesystem-and-manifest-inspection"
detected = ["cargo-lockfile", "mise-rust-toolchain", "renovate-configuration", "rust-binaries:app-a:1", "rust-binaries:app-b:1", "rust-binaries:l2-contract:1", "rust-binaries:verifier:1", "rust-build-script:fixture-harness", "rust-test-targets:fixture-harness:1", "rust-test-targets:verifier:1", "rust-workspace-members:6"]
limitations = ["Project code, build scripts, task runners, and commands are never executed during analysis.", "Release, signing, registry, deployment, branch-protection, and runner-capability contracts remain explicit manual inputs.", "Renovate credentials, runner placement, and write permissions cannot be inferred from repository files.", "Rust build scripts are watched but their system dependencies and generated outputs are not inferred."]

[workflow]
github_runner = "ubuntu-26.04"
velnor_labels = ["velnor-cp-queue-validation", "velnor-target-mvp"]
files = ["_actions-suite.yml", "_docker-suite.yml", "_runtime-suite.yml", "_rust-suite.yml", "app-token-probe.yml", "attestation-negative.yml", "backend-parity.yml", "ci.yml", "compat-public-unmerged.yml", "compat.yml", "control-plane.yml", "docker-lease-probe.yml", "docker.yml", "fixture-rust-check.yml", "l2-negative.yml", "l2-provenance.yml", "l2-runtime.yml", "multi-arch.yml", "pages.yml", "renovate.yml", "reuse-caller.yml", "schedule.yml"]
notes = ["Rust verification uses Mr. Boxington 1.8.3 by default on both lanes. GitHub-hosted jobs use its GitHub cache backend with stable per-unit keys (exact hits reuse the cache; a new entry is saved only when dependency inputs change). Velnor jobs use an explicit local-backend setup step against the image/runner-provided local store."]

[release]
enabled = false
reason = "Release is fail-closed. Enable only after declaring immutable artifact, registry, provenance, and tag-protection policy."

[[unit]]
id = "docker"
label = "Docker"
kind = "docker"
root = "."
watch = ["**"]
github_pr_commands = ["docker build --file 'Dockerfile.fixture' --tag local-ci:dockerfile-fixture '.'"]
github_full_commands = ["docker build --file 'Dockerfile.fixture' --tag local-ci:dockerfile-fixture '.'"]
velnor_pr_commands = ["docker build --file 'Dockerfile.fixture' --tag local-ci:dockerfile-fixture '.'"]
velnor_full_commands = ["docker build --file 'Dockerfile.fixture' --tag local-ci:dockerfile-fixture '.'"]

[[unit]]
id = "docs"
label = "Documentation"
kind = "docs"
root = "."
watch = ["**/*.md", "docs/**", "mkdocs.yml"]
github_pr_commands = ["npx --yes markdownlint-cli2@0.20.0 \"**/*.md\" \"#node_modules\" \"#**/AGENTS.md\" \"#**/CLAUDE.md\" \"#target\" \"#**/target/**\" \"#dist\" \"#coverage\" \"#**/.cache/**\""]
github_full_commands = ["npx --yes markdownlint-cli2@0.20.0 \"**/*.md\" \"#node_modules\" \"#**/AGENTS.md\" \"#**/CLAUDE.md\" \"#target\" \"#**/target/**\" \"#dist\" \"#coverage\" \"#**/.cache/**\""]
velnor_pr_commands = ["npx --yes markdownlint-cli2@0.20.0 \"**/*.md\" \"#node_modules\" \"#**/AGENTS.md\" \"#**/CLAUDE.md\" \"#target\" \"#**/target/**\" \"#dist\" \"#coverage\" \"#**/.cache/**\""]
velnor_full_commands = ["npx --yes markdownlint-cli2@0.20.0 \"**/*.md\" \"#node_modules\" \"#**/AGENTS.md\" \"#**/CLAUDE.md\" \"#target\" \"#**/target/**\" \"#dist\" \"#coverage\" \"#**/.cache/**\""]
[unit.cache]
key_files = ["package-lock.json", "bun.lock"]
paths = ["~/.npm"]

[[unit]]
id = "rust-app-a"
label = "Rust crate (app-a)"
kind = "rust"
root = "crates/app-a"
watch = [".cargo/**", "Cargo.lock", "Cargo.toml", "crates/app-a/**/*.rs", "crates/app-a/Cargo.toml", "crates/app-a/benches/**", "crates/app-a/examples/**", "crates/app-a/src/**", "crates/app-a/tests/**", "mise.lock", "mise.toml", "rust-toolchain", "rust-toolchain.toml"]
github_pr_commands = ["cd -- 'crates/app-a' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/app-a' && mbx clippy --locked --all-targets --all-features --package 'app-a' -- -D warnings", "cd -- 'crates/app-a' && mbx test --locked --all-features --package 'app-a'"]
github_full_commands = ["cd -- 'crates/app-a' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/app-a' && mbx clippy --locked --all-targets --all-features --package 'app-a' -- -D warnings", "cd -- 'crates/app-a' && mbx test --locked --all-features --package 'app-a'"]
velnor_pr_commands = ["cd -- 'crates/app-a' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/app-a' && mbx clippy --locked --all-targets --all-features --package 'app-a' -- -D warnings", "cd -- 'crates/app-a' && mbx test --locked --all-features --package 'app-a'"]
velnor_full_commands = ["cd -- 'crates/app-a' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/app-a' && mbx clippy --locked --all-targets --all-features --package 'app-a' -- -D warnings", "cd -- 'crates/app-a' && mbx test --locked --all-features --package 'app-a'"]
depends_on = ["rust-shared"]
[unit.cache]
key_files = [".cargo/**", "Cargo.toml", "crates/app-a/Cargo.toml", "Cargo.lock", "rust-toolchain.toml", "rust-toolchain"]
paths = ["~/.cargo/registry", "~/.cargo/git"]

[[unit]]
id = "rust-app-b"
label = "Rust crate (app-b)"
kind = "rust"
root = "crates/app-b"
watch = [".cargo/**", "Cargo.lock", "Cargo.toml", "crates/app-b/**/*.rs", "crates/app-b/Cargo.toml", "crates/app-b/benches/**", "crates/app-b/examples/**", "crates/app-b/src/**", "crates/app-b/tests/**", "mise.lock", "mise.toml", "rust-toolchain", "rust-toolchain.toml"]
github_pr_commands = ["cd -- 'crates/app-b' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/app-b' && mbx clippy --locked --all-targets --all-features --package 'app-b' -- -D warnings", "cd -- 'crates/app-b' && mbx test --locked --all-features --package 'app-b'"]
github_full_commands = ["cd -- 'crates/app-b' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/app-b' && mbx clippy --locked --all-targets --all-features --package 'app-b' -- -D warnings", "cd -- 'crates/app-b' && mbx test --locked --all-features --package 'app-b'"]
velnor_pr_commands = ["cd -- 'crates/app-b' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/app-b' && mbx clippy --locked --all-targets --all-features --package 'app-b' -- -D warnings", "cd -- 'crates/app-b' && mbx test --locked --all-features --package 'app-b'"]
velnor_full_commands = ["cd -- 'crates/app-b' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/app-b' && mbx clippy --locked --all-targets --all-features --package 'app-b' -- -D warnings", "cd -- 'crates/app-b' && mbx test --locked --all-features --package 'app-b'"]
depends_on = ["rust-shared"]
[unit.cache]
key_files = [".cargo/**", "Cargo.toml", "crates/app-b/Cargo.toml", "Cargo.lock", "rust-toolchain.toml", "rust-toolchain"]
paths = ["~/.cargo/registry", "~/.cargo/git"]

[[unit]]
id = "rust-fixture-harness"
label = "Rust crate (fixture-harness)"
kind = "rust"
root = "crates/fixture-harness"
watch = [".cargo/**", "Cargo.lock", "Cargo.toml", "crates/fixture-harness/**/*.rs", "crates/fixture-harness/Cargo.toml", "crates/fixture-harness/benches/**", "crates/fixture-harness/build.rs", "crates/fixture-harness/examples/**", "crates/fixture-harness/src/**", "crates/fixture-harness/tests/**", "mise.lock", "mise.toml", "rust-toolchain", "rust-toolchain.toml"]
github_pr_commands = ["cd -- 'crates/fixture-harness' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/fixture-harness' && mbx clippy --locked --all-targets --all-features --package 'fixture-harness' -- -D warnings", "cd -- 'crates/fixture-harness' && mbx test --locked --all-features --package 'fixture-harness'"]
github_full_commands = ["cd -- 'crates/fixture-harness' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/fixture-harness' && mbx clippy --locked --all-targets --all-features --package 'fixture-harness' -- -D warnings", "cd -- 'crates/fixture-harness' && mbx test --locked --all-features --package 'fixture-harness'"]
velnor_pr_commands = ["cd -- 'crates/fixture-harness' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/fixture-harness' && mbx clippy --locked --all-targets --all-features --package 'fixture-harness' -- -D warnings", "cd -- 'crates/fixture-harness' && mbx test --locked --all-features --package 'fixture-harness'"]
velnor_full_commands = ["cd -- 'crates/fixture-harness' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/fixture-harness' && mbx clippy --locked --all-targets --all-features --package 'fixture-harness' -- -D warnings", "cd -- 'crates/fixture-harness' && mbx test --locked --all-features --package 'fixture-harness'"]
[unit.cache]
key_files = [".cargo/**", "Cargo.toml", "crates/fixture-harness/Cargo.toml", "Cargo.lock", "rust-toolchain.toml", "rust-toolchain"]
paths = ["~/.cargo/registry", "~/.cargo/git"]

[[unit]]
id = "rust-l2-contract"
label = "Rust crate (l2-contract)"
kind = "rust"
root = "crates/l2-contract"
watch = [".cargo/**", "Cargo.lock", "Cargo.toml", "crates/l2-contract/**/*.rs", "crates/l2-contract/Cargo.toml", "crates/l2-contract/benches/**", "crates/l2-contract/examples/**", "crates/l2-contract/src/**", "crates/l2-contract/tests/**", "fixtures/l2/closure.json", "fixtures/l2/invalid-mise.lock", "fixtures/l2/mutable-ref.json", "fixtures/l2/unknown-input.json", "fixtures/l2/unknown-repository.json", "fixtures/l2/unknown-subpath.json", "fixtures/l2/unresolved-expression.json", "mise.lock", "mise.toml", "rust-toolchain", "rust-toolchain.toml"]
github_pr_commands = ["cd -- 'crates/l2-contract' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/l2-contract' && mbx clippy --locked --all-targets --all-features --package 'l2-contract' -- -D warnings", "cd -- 'crates/l2-contract' && mbx test --locked --all-features --package 'l2-contract'"]
github_full_commands = ["cd -- 'crates/l2-contract' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/l2-contract' && mbx clippy --locked --all-targets --all-features --package 'l2-contract' -- -D warnings", "cd -- 'crates/l2-contract' && mbx test --locked --all-features --package 'l2-contract'"]
velnor_pr_commands = ["cd -- 'crates/l2-contract' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/l2-contract' && mbx clippy --locked --all-targets --all-features --package 'l2-contract' -- -D warnings", "cd -- 'crates/l2-contract' && mbx test --locked --all-features --package 'l2-contract'"]
velnor_full_commands = ["cd -- 'crates/l2-contract' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/l2-contract' && mbx clippy --locked --all-targets --all-features --package 'l2-contract' -- -D warnings", "cd -- 'crates/l2-contract' && mbx test --locked --all-features --package 'l2-contract'"]
[unit.cache]
key_files = [".cargo/**", "Cargo.toml", "crates/l2-contract/Cargo.toml", "Cargo.lock", "rust-toolchain.toml", "rust-toolchain"]
paths = ["~/.cargo/registry", "~/.cargo/git"]

[[unit]]
id = "rust-shared"
label = "Rust crate (shared)"
kind = "rust"
root = "crates/shared"
watch = [".cargo/**", "Cargo.lock", "Cargo.toml", "crates/shared/**/*.rs", "crates/shared/Cargo.toml", "crates/shared/benches/**", "crates/shared/examples/**", "crates/shared/src/**", "crates/shared/tests/**", "mise.lock", "mise.toml", "rust-toolchain", "rust-toolchain.toml"]
github_pr_commands = ["cd -- 'crates/shared' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/shared' && mbx clippy --locked --all-targets --all-features --package 'shared' -- -D warnings", "cd -- 'crates/shared' && mbx test --locked --all-features --package 'shared'"]
github_full_commands = ["cd -- 'crates/shared' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/shared' && mbx clippy --locked --all-targets --all-features --package 'shared' -- -D warnings", "cd -- 'crates/shared' && mbx test --locked --all-features --package 'shared'"]
velnor_pr_commands = ["cd -- 'crates/shared' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/shared' && mbx clippy --locked --all-targets --all-features --package 'shared' -- -D warnings", "cd -- 'crates/shared' && mbx test --locked --all-features --package 'shared'"]
velnor_full_commands = ["cd -- 'crates/shared' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/shared' && mbx clippy --locked --all-targets --all-features --package 'shared' -- -D warnings", "cd -- 'crates/shared' && mbx test --locked --all-features --package 'shared'"]
[unit.cache]
key_files = [".cargo/**", "Cargo.toml", "crates/shared/Cargo.toml", "Cargo.lock", "rust-toolchain.toml", "rust-toolchain"]
paths = ["~/.cargo/registry", "~/.cargo/git"]

[[unit]]
id = "rust-verifier"
label = "Rust crate (verifier)"
kind = "rust"
root = "crates/verifier"
watch = [".cargo/**", "Cargo.lock", "Cargo.toml", "crates/verifier/**/*.rs", "crates/verifier/Cargo.toml", "crates/verifier/benches/**", "crates/verifier/examples/**", "crates/verifier/src/**", "crates/verifier/tests/**", "mise.lock", "mise.toml", "rust-toolchain", "rust-toolchain.toml"]
github_pr_commands = ["cd -- 'crates/verifier' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/verifier' && mbx clippy --locked --all-targets --all-features --package 'verifier' -- -D warnings", "cd -- 'crates/verifier' && mbx test --locked --all-features --package 'verifier'"]
github_full_commands = ["cd -- 'crates/verifier' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/verifier' && mbx clippy --locked --all-targets --all-features --package 'verifier' -- -D warnings", "cd -- 'crates/verifier' && mbx test --locked --all-features --package 'verifier'"]
velnor_pr_commands = ["cd -- 'crates/verifier' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/verifier' && mbx clippy --locked --all-targets --all-features --package 'verifier' -- -D warnings", "cd -- 'crates/verifier' && mbx test --locked --all-features --package 'verifier'"]
velnor_full_commands = ["cd -- 'crates/verifier' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/verifier' && mbx clippy --locked --all-targets --all-features --package 'verifier' -- -D warnings", "cd -- 'crates/verifier' && mbx test --locked --all-features --package 'verifier'"]
[unit.cache]
key_files = [".cargo/**", "Cargo.toml", "crates/verifier/Cargo.toml", "Cargo.lock", "rust-toolchain.toml", "rust-toolchain"]
paths = ["~/.cargo/registry", "~/.cargo/git"]
