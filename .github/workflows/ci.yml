# Fleet fan-out canary caller for velnor-actions-fixture.
#
# One-purpose caller that mirrors the owner-local fan-out canary shape and pins
# all three owners' fanout-smoke reusable workflow at the immutable fleet
# release. Exactly the local owner's call runs (this repo is owned by tailrocks);
# the other two skip. ci-required is fail-closed with the positive truth table.
# The three job-level uses lines share one SHA pin and one CalVer comment so the
# built-in Renovate github-actions manager updates all three together.
name: CI

on:
  pull_request:
  push:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  jackin-project:
    name: jackin-project
    if: ${{ github.repository_owner == 'jackin-project' }}
    permissions:
      contents: read
    uses: jackin-project/velnor-actions/.github/workflows/fanout-smoke.yml@2fb9caed5ad1462f3555d9acc78ea05024bf0931 # 2026.7.0

  tailrocks:
    name: tailrocks
    if: ${{ github.repository_owner == 'tailrocks' }}
    permissions:
      contents: read
    uses: tailrocks/velnor-actions/.github/workflows/fanout-smoke.yml@2fb9caed5ad1462f3555d9acc78ea05024bf0931 # 2026.7.0

  ChainArgos:
    name: ChainArgos
    if: ${{ github.repository_owner == 'ChainArgos' }}
    permissions:
      contents: read
    uses: ChainArgos/velnor-actions/.github/workflows/fanout-smoke.yml@2fb9caed5ad1462f3555d9acc78ea05024bf0931 # 2026.7.0

  ci-required:
    name: ci-required
    needs:
      - jackin-project
      - tailrocks
      - ChainArgos
    if: ${{ always() }}
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      contents: read
    steps:
      - name: Enforce fleet contract
        env:
          OWNER: ${{ github.repository_owner }}
          JACKIN_PROJECT_RESULT: ${{ needs.jackin-project.result }}
          JACKIN_PROJECT_CONTRACT: ${{ needs.jackin-project.outputs.contract }}
          TAILROCKS_RESULT: ${{ needs.tailrocks.result }}
          TAILROCKS_CONTRACT: ${{ needs.tailrocks.outputs.contract }}
          CHAINARGOS_RESULT: ${{ needs.ChainArgos.result }}
          CHAINARGOS_CONTRACT: ${{ needs.ChainArgos.outputs.contract }}
        shell: bash
        run: |
          set -euo pipefail
          # Positive truth table: accept ONLY a recognized owner whose selected call
          # succeeded with an explicit contract=success output, and whose two other owner
          # calls skipped with empty outputs. No "no failure found" logic; skipped-success
          # is never acceptance.
          case "${OWNER}" in
            jackin-project)
              sel_result="${JACKIN_PROJECT_RESULT}"; sel_contract="${JACKIN_PROJECT_CONTRACT}"
              a_result="${TAILROCKS_RESULT}"; a_contract="${TAILROCKS_CONTRACT}"
              b_result="${CHAINARGOS_RESULT}"; b_contract="${CHAINARGOS_CONTRACT}"
              ;;
            tailrocks)
              sel_result="${TAILROCKS_RESULT}"; sel_contract="${TAILROCKS_CONTRACT}"
              a_result="${JACKIN_PROJECT_RESULT}"; a_contract="${JACKIN_PROJECT_CONTRACT}"
              b_result="${CHAINARGOS_RESULT}"; b_contract="${CHAINARGOS_CONTRACT}"
              ;;
            ChainArgos)
              sel_result="${CHAINARGOS_RESULT}"; sel_contract="${CHAINARGOS_CONTRACT}"
              a_result="${JACKIN_PROJECT_RESULT}"; a_contract="${JACKIN_PROJECT_CONTRACT}"
              b_result="${TAILROCKS_RESULT}"; b_contract="${TAILROCKS_CONTRACT}"
              ;;
            *)
              echo "ci-required: unrecognized owner '${OWNER}'" >&2
              exit 1
              ;;
          esac
          if [ "${sel_result}" != "success" ]; then
            echo "ci-required: selected owner result is '${sel_result}', expected 'success'" >&2
            exit 1
          fi
          if [ "${sel_contract}" != "success" ]; then
            echo "ci-required: selected owner contract is '${sel_contract}', expected 'success'" >&2
            exit 1
          fi
          if [ "${a_result}" != "skipped" ] || [ "${b_result}" != "skipped" ]; then
            echo "ci-required: a non-selected owner ran (results '${a_result}', '${b_result}'), expected both 'skipped'" >&2
            exit 1
          fi
          if [ -n "${a_contract}" ] || [ -n "${b_contract}" ]; then
            echo "ci-required: a non-selected owner produced a contract output, expected empty" >&2
            exit 1
          fi
          echo "ci-required: fleet contract satisfied for owner '${OWNER}'"
