# Generated by velnor-workflow. Regenerate; do not hand-edit.
name: CI / PR
run-name: CI / PR · ${{ github.event_name }} · ${{ github.ref_name }}

on:
  pull_request:
  workflow_dispatch:
    inputs:
      scope:
        description: Verification scope
        required: true
        default: affected
        type: choice
        options:
          - affected
          - full
      base_sha:
        description: Git ref or SHA used as the affected-selection base
        required: false
        default: refs/heads/main
        type: string


concurrency:
  group: ci-${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

permissions:
  actions: read
  contents: read

jobs:
  plan:
    name: "Control / Planning"
    runs-on: ubuntu-24.04
    outputs:
      scope: ${{ steps.plan.outputs.scope }}
      base_sha: ${{ steps.plan.outputs.base_sha }}
      head_sha: ${{ steps.plan.outputs.head_sha }}
      units: ${{ steps.plan.outputs.units }}
      unit_ids: ${{ steps.plan.outputs.unit_ids }}
      full_units: ${{ steps.plan.outputs.full_units }}
      plan_digest: ${{ steps.plan.outputs.plan_digest }}
      excluded: ${{ steps.plan.outputs.excluded }}
      docker_matrix: ${{ steps.plan.outputs.docker_matrix }}
      rust_matrix: ${{ steps.plan.outputs.rust_matrix }}
      # Presence-only no-work marker: `planned_no_work` is `true` or absent,
      # never `false`. Consumers MUST branch
      # `needs.plan.outputs.planned_no_work == 'true'` for the no-work path;
      # any other value (including absent) means the plan selected work.
      planned_no_work: ${{ steps.plan.outputs.planned_no_work }}
      no_work_reason: ${{ steps.plan.outputs.no_work_reason }}
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0
          persist-credentials: false
      - name: Set up Velnor workflow runtime
        id: runtime
        uses: tailrocks/velnor/.github/actions/setup-velnor-workflow@be61acbbbfb71db6b51337e547e39de049d4fc15
        with:
          rev: be61acbbbfb71db6b51337e547e39de049d4fc15
      - name: Set trusted workflow policy revision
        run: echo "VELNOR_WORKFLOW_POLICY_REVISION=be61acbbbfb71db6b51337e547e39de049d4fc15" >> "$GITHUB_ENV"
      - name: Select affected units
        id: plan
        env:
          EVENT_NAME: ${{ github.event_name }}
          CI_SCOPE_OVERRIDE: ${{ github.event.inputs.scope || '' }}
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.inputs.base_sha || github.event.before || 'refs/heads/main' }}
          HEAD_SHA: ${{ github.sha }}
          VELNOR_PROVIDERS: github-hosted
          VELNOR_EVENT_TRUSTED: ${{ (!(github.event_name == 'pull_request' && (github.event.pull_request.head.repo.fork || github.event.pull_request.user.type == 'Bot'))) && 'true' || 'false' }}
          VELNOR_EXPECTED_WORK_FILE: .velnor-ci-expected-work/expected-work.json
        run: |
          set -euo pipefail
          if [[ -z "${CI_SCOPE_OVERRIDE:-}" ]]; then unset CI_SCOPE_OVERRIDE; fi
          mkdir -p .velnor-ci-expected-work
          velnor-workflow plan --config .github/ci/project.toml

      - name: Publish expected work
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: velnor-expected-work
          path: .velnor-ci-expected-work/expected-work.json
          if-no-files-found: error
          retention-days: 7
      - name: Prepare Velnor workflow runtime
        shell: bash
        env:
          EXPECTED_REVISION: be61acbbbfb71db6b51337e547e39de049d4fc15
        run: |
          set -euo pipefail
          stage="$RUNNER_TEMP/velnor-workflow-runtime"
          rm -rf "$stage"
          mkdir -p "$stage"
          src="$(command -v velnor-workflow)"
          install -m 0755 "$src" "$stage/velnor-workflow"
          digest="$(sha256sum "$stage/velnor-workflow" | awk '{print $1}')"
          policy_src="${VELNOR_WORKFLOW_PINNED_BINARY:-$src}"
          install -m 0755 "$policy_src" "$stage/velnor-workflow-policy"
          policy_revision="$("$stage/velnor-workflow-policy" --revision)"
          policy_closure="$("$stage/velnor-workflow-policy" --closure)"
          [[ "$policy_closure" == "${{ steps.runtime.outputs.closure }}" ]] || { echo "::error::policy runtime reports closure $policy_closure, expected ${{ steps.runtime.outputs.closure }}" >&2; exit 1; }
          policy_digest="$(sha256sum "$stage/velnor-workflow-policy" | awk '{print $1}')"
          jq -n --arg repository "$GITHUB_REPOSITORY" --arg revision "$EXPECTED_REVISION" --arg closure "${{ steps.runtime.outputs.closure }}" --arg head_branch "${{ github.ref_name }}" --arg platform "${{ runner.os }}-${{ runner.arch }}" --arg run_id "$GITHUB_RUN_ID" --arg job_id "${{ github.job }}" --arg binary_sha256 "$digest" --arg policy_revision "$policy_revision" --arg policy_closure "$policy_closure" --arg policy_binary_sha256 "$policy_digest" '{repository: $repository, revision: $revision, closure: $closure, head_branch: $head_branch, platform: $platform, run_id: $run_id, job_id: $job_id, binary_sha256: $binary_sha256, policy_revision: $policy_revision, policy_closure: $policy_closure, policy_binary_sha256: $policy_binary_sha256}' > "$stage/manifest.json"
      - name: Publish Velnor workflow runtime
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: velnor-workflow-runtime-be61acbbbfb71db6b51337e547e39de049d4fc15-${{ runner.os }}-${{ runner.arch }}
          path: ${{ runner.temp }}/velnor-workflow-runtime
          if-no-files-found: error
          retention-days: 7
  github-hosted-docker:
    name: "Docker · Docker · github-hosted — docker"
    if: ${{ !cancelled() && needs.plan.result == 'success' && contains(needs.plan.outputs.units, '"unit_id":"docker"') && (true) }}
    needs: [plan]
    uses: ./.github/workflows/ci-unit-docker.yml
    with:
      unit: docker
      provider: github-hosted
      selected_units: ${{ needs.plan.outputs.units }}
      selected_unit_ids: ${{ needs.plan.outputs.unit_ids }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      plan_digest: ${{ needs.plan.outputs.plan_digest }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      unit_platform: linux-x64
      unit_trust: untrusted-ok
      unit_admission: github-hosted
  github-hosted-rust-app-a:
    name: "Rust · app-a · github-hosted — rust-app-a"
    if: ${{ !cancelled() && needs.plan.result == 'success' && contains(needs.plan.outputs.units, '"unit_id":"rust-app-a"') && (true) }}
    needs: [plan]
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-app-a
      provider: github-hosted
      selected_units: ${{ needs.plan.outputs.units }}
      selected_unit_ids: ${{ needs.plan.outputs.unit_ids }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      plan_digest: ${{ needs.plan.outputs.plan_digest }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      mbx_enabled: true
      mbx_compat: "56fcf44fb7c0"
      mbx_dependency_files: |
        .cargo/**
        Cargo.lock
        rust-toolchain
        rust-toolchain.toml
      mbx_freshness_files: |
        crates/app-a/**/*.rs
        crates/app-a/benches/**
        crates/app-a/examples/**
        crates/app-a/src/**
        crates/app-a/tests/**
        crates/shared/**/*.rs
        crates/shared/benches/**
        crates/shared/examples/**
        crates/shared/src/**
        crates/shared/tests/**
        mise.lock
      cache_paths: |
        ~/.cargo/registry
        ~/.cargo/git
      cache_key_files: |
        .cargo/**
        Cargo.lock
        rust-toolchain.toml
        rust-toolchain
      cargo_root: "crates/app-a"
      cargo_net_offline: true
      unit_platform: linux-x64
      unit_trust: untrusted-ok
      unit_dependencies: rust-shared
      unit_admission: github-hosted
      validation_phases: "fmt,clippy,test"
  github-hosted-rust-app-b:
    name: "Rust · app-b · github-hosted — rust-app-b"
    if: ${{ !cancelled() && needs.plan.result == 'success' && contains(needs.plan.outputs.units, '"unit_id":"rust-app-b"') && (true) }}
    needs: [plan]
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-app-b
      provider: github-hosted
      selected_units: ${{ needs.plan.outputs.units }}
      selected_unit_ids: ${{ needs.plan.outputs.unit_ids }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      plan_digest: ${{ needs.plan.outputs.plan_digest }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      mbx_enabled: true
      mbx_compat: "69474d0b2c6d"
      mbx_dependency_files: |
        .cargo/**
        Cargo.lock
        rust-toolchain
        rust-toolchain.toml
      mbx_freshness_files: |
        crates/app-b/**/*.rs
        crates/app-b/benches/**
        crates/app-b/examples/**
        crates/app-b/src/**
        crates/app-b/tests/**
        crates/shared/**/*.rs
        crates/shared/benches/**
        crates/shared/examples/**
        crates/shared/src/**
        crates/shared/tests/**
        mise.lock
      cache_paths: |
        ~/.cargo/registry
        ~/.cargo/git
      cache_key_files: |
        .cargo/**
        Cargo.lock
        rust-toolchain.toml
        rust-toolchain
      cargo_root: "crates/app-b"
      cargo_net_offline: true
      unit_platform: linux-x64
      unit_trust: untrusted-ok
      unit_dependencies: rust-shared
      unit_admission: github-hosted
      validation_phases: "fmt,clippy,test"
  github-hosted-rust-fixture-harness:
    name: "Rust · fixture-harness · github-hosted — rust-fixture-harness"
    if: ${{ !cancelled() && needs.plan.result == 'success' && contains(needs.plan.outputs.units, '"unit_id":"rust-fixture-harness"') && (true) }}
    needs: [plan]
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-fixture-harness
      provider: github-hosted
      selected_units: ${{ needs.plan.outputs.units }}
      selected_unit_ids: ${{ needs.plan.outputs.unit_ids }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      plan_digest: ${{ needs.plan.outputs.plan_digest }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      mbx_enabled: true
      mbx_compat: "47eb42dbd521"
      mbx_dependency_files: |
        .cargo/**
        Cargo.lock
        rust-toolchain
        rust-toolchain.toml
      mbx_freshness_files: |
        crates/fixture-harness/**/*.rs
        crates/fixture-harness/benches/**
        crates/fixture-harness/build.rs
        crates/fixture-harness/examples/**
        crates/fixture-harness/src/**
        crates/fixture-harness/tests/**
        mise.lock
      cache_paths: |
        ~/.cargo/registry
        ~/.cargo/git
      cache_key_files: |
        .cargo/**
        Cargo.lock
        rust-toolchain.toml
        rust-toolchain
      cargo_root: "crates/fixture-harness"
      cargo_net_offline: true
      unit_platform: linux-x64
      unit_trust: untrusted-ok
      unit_admission: github-hosted
      validation_phases: "fmt,clippy,test"
  github-hosted-rust-l2-contract:
    name: "Rust · l2-contract · github-hosted — rust-l2-contract"
    if: ${{ !cancelled() && needs.plan.result == 'success' && contains(needs.plan.outputs.units, '"unit_id":"rust-l2-contract"') && (true) }}
    needs: [plan]
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-l2-contract
      provider: github-hosted
      selected_units: ${{ needs.plan.outputs.units }}
      selected_unit_ids: ${{ needs.plan.outputs.unit_ids }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      plan_digest: ${{ needs.plan.outputs.plan_digest }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      mbx_enabled: true
      mbx_compat: "512a6ab2659c"
      mbx_dependency_files: |
        .cargo/**
        Cargo.lock
        rust-toolchain
        rust-toolchain.toml
      mbx_freshness_files: |
        crates/l2-contract/**/*.rs
        crates/l2-contract/benches/**
        crates/l2-contract/examples/**
        crates/l2-contract/src/**
        crates/l2-contract/tests/**
        fixtures/l2/closure.json
        fixtures/l2/invalid-mise.lock
        fixtures/l2/mutable-ref.json
        fixtures/l2/unknown-input.json
        fixtures/l2/unknown-repository.json
        fixtures/l2/unknown-subpath.json
        fixtures/l2/unresolved-expression.json
        mise.lock
      cache_paths: |
        ~/.cargo/registry
        ~/.cargo/git
      cache_key_files: |
        .cargo/**
        Cargo.lock
        rust-toolchain.toml
        rust-toolchain
      cargo_root: "crates/l2-contract"
      cargo_net_offline: true
      unit_platform: linux-x64
      unit_trust: untrusted-ok
      unit_admission: github-hosted
      validation_phases: "fmt,clippy,test"
  github-hosted-rust-shared:
    name: "Rust · shared · github-hosted — rust-shared"
    if: ${{ !cancelled() && needs.plan.result == 'success' && contains(needs.plan.outputs.units, '"unit_id":"rust-shared"') && (true) }}
    needs: [plan]
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-shared
      provider: github-hosted
      selected_units: ${{ needs.plan.outputs.units }}
      selected_unit_ids: ${{ needs.plan.outputs.unit_ids }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      plan_digest: ${{ needs.plan.outputs.plan_digest }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      mbx_enabled: true
      mbx_compat: "013df28535e3"
      mbx_dependency_files: |
        .cargo/**
        Cargo.lock
        rust-toolchain
        rust-toolchain.toml
      mbx_freshness_files: |
        crates/shared/**/*.rs
        crates/shared/benches/**
        crates/shared/examples/**
        crates/shared/src/**
        crates/shared/tests/**
        mise.lock
      cache_paths: |
        ~/.cargo/registry
        ~/.cargo/git
      cache_key_files: |
        .cargo/**
        Cargo.lock
        rust-toolchain.toml
        rust-toolchain
      cargo_root: "crates/shared"
      cargo_net_offline: true
      unit_platform: linux-x64
      unit_trust: untrusted-ok
      unit_admission: github-hosted
      validation_phases: "fmt,clippy,test"
  github-hosted-rust-verifier:
    name: "Rust · verifier · github-hosted — rust-verifier"
    if: ${{ !cancelled() && needs.plan.result == 'success' && contains(needs.plan.outputs.units, '"unit_id":"rust-verifier"') && (true) }}
    needs: [plan]
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-verifier
      provider: github-hosted
      selected_units: ${{ needs.plan.outputs.units }}
      selected_unit_ids: ${{ needs.plan.outputs.unit_ids }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      plan_digest: ${{ needs.plan.outputs.plan_digest }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      mbx_enabled: true
      mbx_compat: "5267b2856173"
      mbx_dependency_files: |
        .cargo/**
        Cargo.lock
        rust-toolchain
        rust-toolchain.toml
      mbx_freshness_files: |
        crates/verifier/**/*.rs
        crates/verifier/benches/**
        crates/verifier/examples/**
        crates/verifier/src/**
        crates/verifier/tests/**
        mise.lock
      cache_paths: |
        ~/.cargo/registry
        ~/.cargo/git
      cache_key_files: |
        .cargo/**
        Cargo.lock
        rust-toolchain.toml
        rust-toolchain
      cargo_root: "crates/verifier"
      cargo_net_offline: true
      unit_platform: linux-x64
      unit_trust: untrusted-ok
      unit_admission: github-hosted
      validation_phases: "fmt,clippy,test"
  ci-required:
    name: ci-required
    if: ${{ !cancelled() }}
    needs: [plan, github-hosted-docker, github-hosted-rust-app-a, github-hosted-rust-app-b, github-hosted-rust-fixture-harness, github-hosted-rust-l2-contract, github-hosted-rust-shared, github-hosted-rust-verifier]
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    steps:
      - name: Download Velnor workflow runtime
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: velnor-workflow-runtime-be61acbbbfb71db6b51337e547e39de049d4fc15-${{ runner.os }}-${{ runner.arch }}
          path: .velnor-workflow-runtime
      - name: Verify Velnor workflow runtime
        shell: bash
        env:
          EXPECTED_REVISION: be61acbbbfb71db6b51337e547e39de049d4fc15
        run: |
          set -euo pipefail
          manifest=.velnor-workflow-runtime/manifest.json
          jq -e --arg revision "$EXPECTED_REVISION" --arg repository "$GITHUB_REPOSITORY" --arg platform "${RUNNER_OS}-${RUNNER_ARCH}" --arg run_id "$GITHUB_RUN_ID" '.revision == $revision and .repository == $repository and .platform == $platform and .run_id == $run_id and (.run_id | test("^[0-9]+$")) and .job_id != "" and (.binary_sha256 | test("^[0-9a-f]{64}$")) and (.policy_binary_sha256 | test("^[0-9a-f]{64}$")) and (.closure | test("^[0-9a-f]{64}$")) and (.policy_closure | test("^[0-9a-f]{64}$"))' "$manifest" >/dev/null
          expected="$(jq -er '.binary_sha256' "$manifest")"
          actual="$(sha256sum .velnor-workflow-runtime/velnor-workflow | awk '{print $1}')"
          [[ "$actual" == "$expected" ]] || { echo "::error::runtime digest mismatch" >&2; exit 1; }
          expected="$(jq -er '.policy_binary_sha256' "$manifest")"
          actual="$(sha256sum .velnor-workflow-runtime/velnor-workflow-policy | awk '{print $1}')"
          [[ "$actual" == "$expected" ]] || { echo "::error::policy runtime digest mismatch" >&2; exit 1; }
      - name: Add Velnor workflow runtime to PATH
        shell: bash
        env:
          EXPECTED_REVISION: be61acbbbfb71db6b51337e547e39de049d4fc15
        run: |
          set -euo pipefail
          home="$RUNNER_TEMP/velnor-workflow-runtime-artifact"
          install -Dm0755 .velnor-workflow-runtime/velnor-workflow "$home/bin/velnor-workflow"
          install -Dm0755 .velnor-workflow-runtime/velnor-workflow-policy "$home/bin/velnor-workflow-policy"
          expected_closure="$(jq -er '.policy_closure' .velnor-workflow-runtime/manifest.json)"
          reported="$("$home/bin/velnor-workflow-policy" --closure)"
          [[ "$reported" == "$expected_closure" ]] || { echo "::error::policy runtime reports closure $reported, expected $expected_closure" >&2; exit 1; }
          echo "$home/bin" >> "$GITHUB_PATH"
          echo "VELNOR_WORKFLOW_PINNED_BINARY=$home/bin/velnor-workflow-policy" >> "$GITHUB_ENV"
      - name: Download expected work
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: velnor-expected-work
          path: .velnor-ci-expected-work
      - name: Download reported unit results
        # A no-work plan runs no unit jobs, so zero result artifacts is the
        # expected case there — and the aggregate fails a real-work plan with
        # zero records anyway. Tolerate the empty download; never the verdict.
        continue-on-error: true
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          pattern: velnor-result-*
          merge-multiple: true
          path: .velnor-ci-results
      - name: Collect reported unit results
        shell: bash
        run: |
          set -euo pipefail
          shopt -s nullglob
          mkdir -p .velnor-ci-results
          files=(.velnor-ci-results/result-*.json)
          for file in "${files[@]}"; do
            if jq -e 'any(.results[]?; has("reused_from"))' "$file" >/dev/null; then
              echo "::error::$file carries reused_from without a validate_reuse decision; render emits no reused results" >&2
              exit 1
            fi
          done
          if (( ${#files[@]} == 0 )); then
            printf '{"results":[]}\n' > .velnor-ci-results.json
          else
            jq -s '{results: ([.[].results // empty] | add // [])}' "${files[@]}" > .velnor-ci-results.json
          fi
          echo "collected $(jq '.results | length' .velnor-ci-results.json) reported result(s) from ${#files[@]} record file(s)"
      - name: Score expected work against reported results
        env:
          BASE_SHA: ${{ needs.plan.outputs.base_sha }}
          HEAD_SHA: ${{ needs.plan.outputs.head_sha }}
        shell: bash
        run: |
          set -euo pipefail
          velnor-workflow aggregate --expected .velnor-ci-expected-work/expected-work.json --results .velnor-ci-results.json
      - name: Validate generated stack results
        env:
          NEEDS_JSON: ${{ toJSON(needs) }}
          SELECTED_UNITS: ${{ needs.plan.outputs.units }}
          PLAN_DIGEST: ${{ needs.plan.outputs.plan_digest }}
          EXCLUDED: ${{ needs.plan.outputs.excluded }}
          EXPECTED_CALLERS: "[{\"job_id\":\"github-hosted-docker\",\"provider\":\"github-hosted\",\"unit_id\":\"docker\"},{\"job_id\":\"github-hosted-rust-app-a\",\"provider\":\"github-hosted\",\"unit_id\":\"rust-app-a\"},{\"job_id\":\"github-hosted-rust-app-b\",\"provider\":\"github-hosted\",\"unit_id\":\"rust-app-b\"},{\"job_id\":\"github-hosted-rust-fixture-harness\",\"provider\":\"github-hosted\",\"unit_id\":\"rust-fixture-harness\"},{\"job_id\":\"github-hosted-rust-l2-contract\",\"provider\":\"github-hosted\",\"unit_id\":\"rust-l2-contract\"},{\"job_id\":\"github-hosted-rust-shared\",\"provider\":\"github-hosted\",\"unit_id\":\"rust-shared\"},{\"job_id\":\"github-hosted-rust-verifier\",\"provider\":\"github-hosted\",\"unit_id\":\"rust-verifier\"}]"
          PROVIDER_ADMITTED_GITHUB_HOSTED: ${{ true }}
        shell: bash
        run: |
          set -euo pipefail
          if [[ -z "$PLAN_DIGEST" ]]; then
            echo "plan did not freeze a plan digest: the expected set has no identity" >&2
            exit 1
          fi
          echo "verdict binds plan digest $PLAN_DIGEST"
          result_for_job() {
            jq -r --arg job "$1" '.[$job].result // empty' <<<"$NEEDS_JSON"
          }
          plan_expects() {
            [[ "$(jq -r --arg unit "$1" --arg provider "$2" '[.[] | select(.unit_id == $unit) | .providers[] | select(. == $provider)] | length' <<<"$SELECTED_UNITS")" -gt 0 ]]
          }
          if ! jq -e --argjson expected "$EXPECTED_CALLERS" '($expected | type == "array") and all($expected[]; type == "object" and (.unit_id | type) == "string" and (.provider | type) == "string" and (.job_id | type) == "string")' -n; then
            echo "generated required-caller contract is malformed" >&2
            exit 1
          fi
          if ! jq -e --argjson expected "$EXPECTED_CALLERS" 'type == "array" and (map(.unit_id) | unique | length) == length and all(.[]; . as $entry | ($entry | type) == "object" and ($entry.unit_id | type) == "string" and ($entry.unit_id | length) > 0 and ($entry.providers | type) == "array" and ($entry.providers | length) > 0 and (($entry.providers | map(type == "string" and length > 0) | all)) and (($entry.providers | unique | length) == ($entry.providers | length)) and all($entry.providers[]; . as $provider | any($expected[]; .unit_id == $entry.unit_id and .provider == $provider)))' <<<"$SELECTED_UNITS" >/dev/null; then
            echo "plan selected-unit output contains an unknown, duplicate, empty, or unmapped obligation" >&2
            exit 1
          fi
          if ! jq -e 'type == "object"' <<<"$NEEDS_JSON" >/dev/null; then
            echo "workflow needs output is malformed" >&2
            exit 1
          fi
          result="$(result_for_job plan)"
          if [[ "$result" != success ]]; then
            echo "required CI prerequisite plan did not pass: $result" >&2
            exit 1
          fi
          if plan_expects "docker" "github-hosted"; then
            result="$(result_for_job github-hosted-docker)"
            if [[ "$PROVIDER_ADMITTED_GITHUB_HOSTED" == true ]]; then
              case "$result" in
                success) ;;
                skipped) echo "expected CI job github-hosted-docker was skipped: a skipped expected result cannot pass" >&2; exit 1 ;;
                cancelled) echo "expected CI job github-hosted-docker was cancelled: a cancelled expected result cannot pass" >&2; exit 1 ;;
                *) echo "expected CI job github-hosted-docker did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                skipped) ;;
                *) echo "selected CI job github-hosted-docker ran outside its provider admission (PROVIDER_ADMITTED_GITHUB_HOSTED=$PROVIDER_ADMITTED_GITHUB_HOSTED): $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job github-hosted-docker)"
            case "$result" in
              skipped) ;;
              success) echo "unexpected CI job github-hosted-docker succeeded outside the expected set: the plan did not declare it" >&2; exit 1 ;;
              *) echo "unexpected CI job github-hosted-docker ran outside the expected set: $result" >&2; exit 1 ;;
            esac
          fi
          if plan_expects "rust-app-a" "github-hosted"; then
            result="$(result_for_job github-hosted-rust-app-a)"
            if [[ "$PROVIDER_ADMITTED_GITHUB_HOSTED" == true ]]; then
              case "$result" in
                success) ;;
                skipped) echo "expected CI job github-hosted-rust-app-a was skipped: a skipped expected result cannot pass" >&2; exit 1 ;;
                cancelled) echo "expected CI job github-hosted-rust-app-a was cancelled: a cancelled expected result cannot pass" >&2; exit 1 ;;
                *) echo "expected CI job github-hosted-rust-app-a did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                skipped) ;;
                *) echo "selected CI job github-hosted-rust-app-a ran outside its provider admission (PROVIDER_ADMITTED_GITHUB_HOSTED=$PROVIDER_ADMITTED_GITHUB_HOSTED): $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job github-hosted-rust-app-a)"
            case "$result" in
              skipped) ;;
              success) echo "unexpected CI job github-hosted-rust-app-a succeeded outside the expected set: the plan did not declare it" >&2; exit 1 ;;
              *) echo "unexpected CI job github-hosted-rust-app-a ran outside the expected set: $result" >&2; exit 1 ;;
            esac
          fi
          if plan_expects "rust-app-b" "github-hosted"; then
            result="$(result_for_job github-hosted-rust-app-b)"
            if [[ "$PROVIDER_ADMITTED_GITHUB_HOSTED" == true ]]; then
              case "$result" in
                success) ;;
                skipped) echo "expected CI job github-hosted-rust-app-b was skipped: a skipped expected result cannot pass" >&2; exit 1 ;;
                cancelled) echo "expected CI job github-hosted-rust-app-b was cancelled: a cancelled expected result cannot pass" >&2; exit 1 ;;
                *) echo "expected CI job github-hosted-rust-app-b did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                skipped) ;;
                *) echo "selected CI job github-hosted-rust-app-b ran outside its provider admission (PROVIDER_ADMITTED_GITHUB_HOSTED=$PROVIDER_ADMITTED_GITHUB_HOSTED): $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job github-hosted-rust-app-b)"
            case "$result" in
              skipped) ;;
              success) echo "unexpected CI job github-hosted-rust-app-b succeeded outside the expected set: the plan did not declare it" >&2; exit 1 ;;
              *) echo "unexpected CI job github-hosted-rust-app-b ran outside the expected set: $result" >&2; exit 1 ;;
            esac
          fi
          if plan_expects "rust-fixture-harness" "github-hosted"; then
            result="$(result_for_job github-hosted-rust-fixture-harness)"
            if [[ "$PROVIDER_ADMITTED_GITHUB_HOSTED" == true ]]; then
              case "$result" in
                success) ;;
                skipped) echo "expected CI job github-hosted-rust-fixture-harness was skipped: a skipped expected result cannot pass" >&2; exit 1 ;;
                cancelled) echo "expected CI job github-hosted-rust-fixture-harness was cancelled: a cancelled expected result cannot pass" >&2; exit 1 ;;
                *) echo "expected CI job github-hosted-rust-fixture-harness did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                skipped) ;;
                *) echo "selected CI job github-hosted-rust-fixture-harness ran outside its provider admission (PROVIDER_ADMITTED_GITHUB_HOSTED=$PROVIDER_ADMITTED_GITHUB_HOSTED): $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job github-hosted-rust-fixture-harness)"
            case "$result" in
              skipped) ;;
              success) echo "unexpected CI job github-hosted-rust-fixture-harness succeeded outside the expected set: the plan did not declare it" >&2; exit 1 ;;
              *) echo "unexpected CI job github-hosted-rust-fixture-harness ran outside the expected set: $result" >&2; exit 1 ;;
            esac
          fi
          if plan_expects "rust-l2-contract" "github-hosted"; then
            result="$(result_for_job github-hosted-rust-l2-contract)"
            if [[ "$PROVIDER_ADMITTED_GITHUB_HOSTED" == true ]]; then
              case "$result" in
                success) ;;
                skipped) echo "expected CI job github-hosted-rust-l2-contract was skipped: a skipped expected result cannot pass" >&2; exit 1 ;;
                cancelled) echo "expected CI job github-hosted-rust-l2-contract was cancelled: a cancelled expected result cannot pass" >&2; exit 1 ;;
                *) echo "expected CI job github-hosted-rust-l2-contract did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                skipped) ;;
                *) echo "selected CI job github-hosted-rust-l2-contract ran outside its provider admission (PROVIDER_ADMITTED_GITHUB_HOSTED=$PROVIDER_ADMITTED_GITHUB_HOSTED): $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job github-hosted-rust-l2-contract)"
            case "$result" in
              skipped) ;;
              success) echo "unexpected CI job github-hosted-rust-l2-contract succeeded outside the expected set: the plan did not declare it" >&2; exit 1 ;;
              *) echo "unexpected CI job github-hosted-rust-l2-contract ran outside the expected set: $result" >&2; exit 1 ;;
            esac
          fi
          if plan_expects "rust-shared" "github-hosted"; then
            result="$(result_for_job github-hosted-rust-shared)"
            if [[ "$PROVIDER_ADMITTED_GITHUB_HOSTED" == true ]]; then
              case "$result" in
                success) ;;
                skipped) echo "expected CI job github-hosted-rust-shared was skipped: a skipped expected result cannot pass" >&2; exit 1 ;;
                cancelled) echo "expected CI job github-hosted-rust-shared was cancelled: a cancelled expected result cannot pass" >&2; exit 1 ;;
                *) echo "expected CI job github-hosted-rust-shared did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                skipped) ;;
                *) echo "selected CI job github-hosted-rust-shared ran outside its provider admission (PROVIDER_ADMITTED_GITHUB_HOSTED=$PROVIDER_ADMITTED_GITHUB_HOSTED): $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job github-hosted-rust-shared)"
            case "$result" in
              skipped) ;;
              success) echo "unexpected CI job github-hosted-rust-shared succeeded outside the expected set: the plan did not declare it" >&2; exit 1 ;;
              *) echo "unexpected CI job github-hosted-rust-shared ran outside the expected set: $result" >&2; exit 1 ;;
            esac
          fi
          if plan_expects "rust-verifier" "github-hosted"; then
            result="$(result_for_job github-hosted-rust-verifier)"
            if [[ "$PROVIDER_ADMITTED_GITHUB_HOSTED" == true ]]; then
              case "$result" in
                success) ;;
                skipped) echo "expected CI job github-hosted-rust-verifier was skipped: a skipped expected result cannot pass" >&2; exit 1 ;;
                cancelled) echo "expected CI job github-hosted-rust-verifier was cancelled: a cancelled expected result cannot pass" >&2; exit 1 ;;
                *) echo "expected CI job github-hosted-rust-verifier did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                skipped) ;;
                *) echo "selected CI job github-hosted-rust-verifier ran outside its provider admission (PROVIDER_ADMITTED_GITHUB_HOSTED=$PROVIDER_ADMITTED_GITHUB_HOSTED): $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job github-hosted-rust-verifier)"
            case "$result" in
              skipped) ;;
              success) echo "unexpected CI job github-hosted-rust-verifier succeeded outside the expected set: the plan did not declare it" >&2; exit 1 ;;
              *) echo "unexpected CI job github-hosted-rust-verifier ran outside the expected set: $result" >&2; exit 1 ;;
            esac
          fi
  required:
    name: "Control / Required"
    if: ${{ !cancelled() }}
    needs: [ci-required]
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    steps:
      - name: Mirror CI / Required
        if: ${{ needs.ci-required.result != 'success' }}
        run: exit 1
